import PopularMoviesList from './PopularMoviesList';

const PopularMovies = () => {
  return (
    <section className="relative z-30 -mt-40 md:-mt-32 sm:-mt-20 bg-black px-4 md:px-8 pb-8">
      <PopularMoviesList />
    </section>
  );
};

export default PopularMovies;